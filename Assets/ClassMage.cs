using UnityEngine;
using System.Collections;

public class ClassMage : MonoBehaviour {

    //PROPERTIES
    //Can not be RaceTrolls
    //Can not be RaceHumans
    //Attack at range (attackRange *= 5)
    //Attack damge (damage *= 1.5)
    //Armor (armor = armor * 1)

    //FUNCTIONS
    //Attacks at range
    //Caused afflictions


    // Use this for initialization
    void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
