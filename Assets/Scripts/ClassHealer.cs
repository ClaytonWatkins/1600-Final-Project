using UnityEngine;
using System.Collections;

public class ClassHealer : MonoBehaviour {

    //PROPERTIES
    //Can not be RaceTrolls
    //Can not be RaceHumans
    //Attack damage (damage *= .75) 
    //Heal at range (attackRange *= 5)
    //Armor (armor = armor * 1.5)

    //FUNCTIONS
    //Heal self and faction at range
    //Cure affliction

    // Use this for initialization
    void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
