using UnityEngine;
using System.Collections;

public class ClassWarrior : MonoBehaviour {

    //PROPERTIES
    //Can not be RaceElves
    //Can not be RaceGoblins
    //Attack in melee (attackRange *= 1)
    //Attack damage (damage *= 2)
    //Armor (armor = armor * 2)

    //FUNCTIONS
    //Attacks in melee
    //Ambush from hidden






    // Use this for initialization
    void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
   
	
	}
}
